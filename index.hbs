{{!< default}}

<header class="page-title {{#if @blog.cover}}has-image" style="background-image: url({{@blog.cover}});"{{else}}"{{/if}}>
    <h1 class="page-title-main">{{@blog.title}}</h1>
    <hr class="page-title-separator" />
    {{{showSubTitle}}}
</header>

<main id="content" class="content" role="main">

        {{! The tag below includes the post loop - partials/loop.hbs }}
        {{> "loop"}}

</main>
